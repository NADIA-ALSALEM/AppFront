specVersion: "v1beta"
serviceId: "med-for-all"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "med-for-all-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
